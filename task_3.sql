-- This command is used to display a list of all non-temporary tables
-- within the currently selected database.

-- So in this I only need to pass the command of SHOW TABLES; 
-- and in the terminal use: mysql -u root -p alx_book_store < task_3.sql

USE alx_book_store;
SHOW TABLES;